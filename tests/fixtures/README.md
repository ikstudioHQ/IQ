# tests/fixtures/

Populated automatically by `tools/prompt_regression_test.py capture <slug>`.
Each subfolder is a captured baseline output package for one topic, used to
diff against future regenerations after a prompt edit. Empty until first use.
