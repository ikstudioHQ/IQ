# Repository Layout — v2.72

Active entry points remain at repository root. Current operator documentation is under `docs/current/`. Historical release evidence is under `archive/` and is not active instruction. Production data is under `production/`; canonical sources remain under `sources/` and phase data directories. Generated/runtime outputs remain derived artifacts.

Local-only `MASTER_PROMPT.md` is intentionally excluded from distribution.
